# Guidelines

- Theme variables like colours live in `src/app.css` as CSS custom properties.
- Set font choice in `html` via `src/app.css`.
- Design style: dark, minimal, component-driven; no default HTML styling.
- Styling order: Components → `app.css` semantic classes → inline CSS → lastly, component-local classes.



